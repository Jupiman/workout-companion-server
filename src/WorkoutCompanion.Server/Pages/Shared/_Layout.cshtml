<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] · Workout Companion</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
    <header class="site-header">
        <a class="brand" asp-page="/Index">Workout Companion</a>
        @if (User.Identity?.IsAuthenticated == true)
        {
            <nav aria-label="Primary navigation">
                <a asp-page="/Index" class="@(Context.Request.Path == "/" ? "active" : null)">Dashboard</a>
                <a asp-page="/History/Index" class="@(Context.Request.Path.StartsWithSegments("/history") || Context.Request.Path.StartsWithSegments("/workouts") ? "active" : null)">History</a>
                <a asp-page="/Progress/Index" class="@(Context.Request.Path.StartsWithSegments("/progress") ? "active" : null)">Progress</a>
                <form method="post" asp-page="/Logout">
                    <button class="link-button" type="submit">Logout</button>
                </form>
            </nav>
        }
    </header>
    <main class="page-shell">
        @RenderBody()
    </main>
</body>
</html>

